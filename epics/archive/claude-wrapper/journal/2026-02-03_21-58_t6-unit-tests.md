# T6: Unit Tests (pytest)

## What was worked on
- T6: Write pytest unit tests for claude_wrapper.py

## What was done
- Created `merlin-bot/test_claude_wrapper.py` with 54 tests across 6 test classes
- Created uv venv at `merlin-bot/.venv/` with pytest installed
- All subprocess calls are mocked — no real API calls

### Test classes and coverage:
1. **TestCommandConstruction** (14 tests): verifies all CLI flags are built correctly, `--dangerously-skip-permissions` always included, `--output-format json` always included, prompt always last, timeout passed through
2. **TestJSONParsing** (8 tests): session_id, result, usage, model parsed from JSON; raw_output preserved; empty stdout handled
3. **TestErrorHandling** (6 tests): non-zero exit, invalid JSON fallback + warning log, missing binary (127), timeout (124), unwritable log dir
4. **TestLogging** (12 tests): log file created, filename contains caller and session_id, log content has all required fields (caller, prompt, stdout, stderr, exit_code, duration, timestamps), error paths also produce logs
5. **TestCLI** (11 tests): all argparse flags parsed correctly, JSON output valid, exit code propagated
6. **TestAPICLILoggingParity** (3 tests): both Python API and CLI create log files with identical structure

## Validation
```
54 passed in 0.22s
```

## Files created
- `merlin-bot/test_claude_wrapper.py`
- `merlin-bot/.venv/` (uv venv with pytest)

## Next steps
- T7: End-to-end validation (live test against real Claude Code)
