# 2026-02-03 22:50 — Discord Skill: T1–T6

## What was worked on
Tasks T1 through T6 of the discord-skill epic.

## What was done

### T1: Fix wrapper cwd to merlin-bot/
- Added `MERLIN_BOT_DIR = Path(__file__).parent.resolve()` to `claude_wrapper.py`
- `subprocess.run()` now passes `cwd=MERLIN_BOT_DIR`
- All 54 existing wrapper tests still pass

### T2: Create discord_send.py with send action
- Created `merlin-bot/discord_send.py` with PEP 723 inline deps (`httpx`, `python-dotenv`)
- `chunk_message()` splits at newlines > spaces > hard cut at 2000 chars
- `send_message()` POSTs to Discord REST API, auto-chunks long messages
- CLI: `uv run discord_send.py send --channel <id> --content "text"`
- JSON output to stdout, clean error handling to stderr

### T3: Add reply and react actions
- `reply_message()` — first chunk includes `message_reference`, rest are regular sends
- `react_message()` — PUT to reactions endpoint, URL-encodes emoji, handles 204 No Content
- Refactored shared logic: `_check_response()` and `_auth_headers()` helpers
- CLI: `reply --channel --message --content` and `react --channel --message --emoji`

### T4: .env.example and .gitignore
- Created `merlin-bot/.env.example` with `DISCORD_BOT_TOKEN=your-token-here`
- Added `.env` to root `.gitignore`

### T5: SKILL.md
- Created `merlin-bot/.claude/skills/discord/SKILL.md`
- `user-invocable: false` — Claude uses it autonomously
- Documents all three actions with exact CLI syntax
- Includes chunking behavior notes and Discord writing style guide

### T6: Unit tests
- 23 tests in `tests/test_discord_send.py`
- Covers: chunking (8), send (6), reply (3), react (3), load_token (2), wrapper cwd (1)
- Full suite: 77 tests, all passing

## Current state
- T1–T6 done and validated
- T7 (live validation) remains — needs a real Discord bot token
- `discord_send.py` is complete with send/reply/react
- SKILL.md is in place at `.claude/skills/discord/`
- No `merlin-bot/CLAUDE.md` yet (deferred to own epic)

## Files created/modified
- `merlin-bot/claude_wrapper.py` — added `MERLIN_BOT_DIR`, `cwd` param
- `merlin-bot/discord_send.py` — new file
- `merlin-bot/.env.example` — new file
- `merlin-bot/.claude/skills/discord/SKILL.md` — new file
- `merlin-bot/tests/test_discord_send.py` — new file
- `.gitignore` — added `.env`
- `epics/discord-skill/` — new epic (requirements, tasks, journal)

## Next steps
- T7: Live validation with a real Discord bot token
